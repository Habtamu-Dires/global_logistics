/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { TextDto } from '../../models/text-dto';

export interface RejectDriverProfile$Params {
  'public-id': string;
      body: TextDto
}

export function rejectDriverProfile(http: HttpClient, rootUrl: string, params: RejectDriverProfile$Params, context?: HttpContext): Observable<StrictHttpResponse<{
}>> {
  const rb = new RequestBuilder(rootUrl, rejectDriverProfile.PATH, 'put');
  if (params) {
    rb.path('public-id', params['public-id'], {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<{
      }>;
    })
  );
}

rejectDriverProfile.PATH = '/drivers/reject/{public-id}';
